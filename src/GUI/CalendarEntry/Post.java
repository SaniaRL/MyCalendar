package GUI.CalendarEntry;

import java.time.LocalDate;

public class Post extends CalendarEntry {
    public Post(Category category, LocalDate date, String content){
        super(category, date, content);
    }
}